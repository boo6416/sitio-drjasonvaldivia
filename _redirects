# Lo que NO debe servirse al público — 2026-08-08
#
# Netlify publica la RAÍZ DEL REPOSITORIO tal cual, así que todo archivo que
# viva aquí queda en internet aunque nadie lo enlace. Comprobado ese día contra
# producción, con 200 OK:
#
#     /_pendiente/LEEME.md              200
#     /_pendiente/textos-6-paginas.json 200
#     /_pendiente/armar-paginas.mjs     200
#     /auditar-arbol.mjs                200
#     /README.md                        200
#
# El detalle que lo retrata: `_pendiente/LEEME.md` empieza con «Listo para
# publicar — NO desplegado». Llevaba publicado desde que se fusionó la rama.
#
# No hay credenciales ahí —se revisó archivo por archivo— así que esto no es una
# fuga de secretos. Lo que sí es: material de trabajo interno a la vista, y
# **contenido indexable** (borradores de los mismos textos que ya están en las
# páginas buenas) en un sitio que se está peleando por posicionar. Google
# penaliza justo eso.
#
# El guion bajo del nombre NO protege nada: para Netlify sólo `_headers` y
# `_redirects` son especiales, `_pendiente` es una carpeta como cualquier otra.
#
# ⚠ Ninguna página referencia un `.mjs` —se comprobó con grep sobre las 30— así
# que bloquear la extensión no rompe nada. Si algún día una página necesita
# cargar un módulo `.mjs`, hay que quitar esa línea.
#
# ⚠⚠ EL SIGNO `!` NO ES ADORNO, Y SIN ÉL ESTO NO HACE NADA.
#
# En Netlify una regla de `_redirects` sólo se aplica cuando NO existe un archivo
# en esa ruta: el archivo estático gana siempre. Como `_pendiente/LEEME.md` sí
# existe, la regla se ignoraba y seguía respondiendo 200. Se publicó sin el `!`,
# se comprobó contra producción, y seguía expuesto — el arreglo parecía hecho y
# no lo estaba. El `!` (shadowing forzado) es lo que hace que la regla mande
# sobre el archivo.
#
# Moraleja para la próxima: una regla de redirección publicada no es una regla
# que aplica. Pídale la URL a producción y mire el código de estado.

# ⚠ Y `/*.mjs` TAMPOCO SIRVE: Netlify no admite comodines de EXTENSIÓN. El `*`
# es un splat que sólo va al final de una ruta, así que `/*.mjs` no coincide con
# nada y `auditar-arbol.mjs` siguió respondiendo 200 después del segundo intento.
# Van uno por uno. Si algún día se agrega otro guion a la raíz, se agrega aquí —
# o mejor, no se pone en la raíz.
# ══════════ surgerypuertovallarta.com — 2026-08-16 ══════════
#
# El Dr. es dueño de ese dominio y hoy NO SIRVE DE NADA. Medido:
#   http://surgerypuertovallarta.com    → 302 Found → www.drjasonvaldivia.com
#   https://surgerypuertovallarta.com   → la conexión EXPIRA. No hay nada.
#
# Dos fallas, y la segunda es la que importa:
#
# 1. Es 302 (temporal). Un 302 le dice a Google «no consolides, el original se
#    queda»: NO transmite autoridad. Tiene que ser 301.
#
# 2. La redirección de URL de Namecheap **no habla HTTPS**. Su servicio de
#    forwarding sólo escucha en el puerto 80; responder en HTTPS exige un
#    certificado, y un dominio que sólo tiene un registro de redirección no lo
#    tiene ni puede tenerlo. Por eso expira. Y como los navegadores de hoy
#    intentan HTTPS primero, la mayoría de quien teclee ese dominio ve un error
#    en vez del sitio. NO se arregla dentro de Namecheap: es un límite del
#    servicio, no una casilla mal puesta.
#
# LA SALIDA es que lo sirva Netlify, que ya publica este sitio y emite
# certificados gratis. En cuanto el dominio apunte aquí, estas reglas mandan.
#
# Va a /en/ y no a la raíz a propósito: «surgery puerto vallarta» es una
# consulta EN INGLÉS y la portada en inglés ya existe. Mandar a un anglófono a
# una página en español es perderlo en el primer segundo.
#
# Se redirige todo a /en/ SIN conservar la ruta (nada de `:splat`): ese dominio
# se registró el 2026-04-14, nunca tuvo sitio y no tiene rutas propias que
# preservar. Un `:splat` sólo fabricaría 404 con aire de intencionales.
#
# ⚠ Igual que arriba: el `!` no es adorno. Sin él la regla se ignora en cuanto
# exista un archivo en esa ruta.
http://surgerypuertovallarta.com/*        https://drjasonvaldivia.com/en/   301!
https://surgerypuertovallarta.com/*       https://drjasonvaldivia.com/en/   301!
http://www.surgerypuertovallarta.com/*    https://drjasonvaldivia.com/en/   301!
https://www.surgerypuertovallarta.com/*   https://drjasonvaldivia.com/en/   301!

/_pendiente/*             /404.html   404!
/README.md                /404.html   404!
/auditar-arbol.mjs        /404.html   404!
/revisar-rutas.mjs        /404.html   404!
/generar-aviso.mjs        /404.html   404!
/leer-docx.mjs            /404.html   404!
